[project]
name = "python-socket-connect-w7"
version = "0.1.0"
description = "Socket programming project"
requires-python = ">=3.8"
dependencies = [
    "pygame>=2.6.1",
]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[tool.uv]
dev-dependencies = []
